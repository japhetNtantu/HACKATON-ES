from authenticator.models import EstiamUser
from authenticator.models import Material
from django.contrib import admin

# Register your models here.
admin.site.register(EstiamUser)
admin.site.register(Material)
